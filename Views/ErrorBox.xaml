<Window x:Class="FlightSimulatorApp.Views.ErrorBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FlightSimulatorApp.Views"
        mc:Ignorable="d"
        Title="Error!" Height="100" Width="200">
    <Grid>
        <TextBlock Foreground="Red" FontSize="14" HorizontalAlignment="Left"  Text="Error:" Margin="5 0 0 0" />
        <TextBlock x:Name="errorTxt" Foreground="Red" Text="{Binding Path = VM_Error, Mode=OneWay,
                UpdateSourceTrigger=PropertyChanged}" Grid.Column="0" Grid.Row="0" HorizontalAlignment="Center" Margin="20,0,0,0" VerticalAlignment="Center" FontSize="14" TextAlignment="Center" />
    </Grid>
</Window>
